# Named spectra-bag roles a plugin's containers use (SPEC_pumpkin_integration.md B.2). Uppercase so
# REFERENCE/SAMPLE align with VirtualCaptureRole and SpectrumSampleType values — a str-Enum member and its
# string value hash/compare equal, so a bag keyed by either resolves the same.
REFERENCE = "REFERENCE"
SAMPLE = "SAMPLE"
TRANSMISSION = "TRANSMISSION"
ABSORPTION = "ABSORPTION"
